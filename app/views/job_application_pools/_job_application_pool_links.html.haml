%td= link_to("Show Applications", pool_applications_path(job_application_pool), title: "Show all job applications belonging to this pool")
%td= link_to("Edit", edit_pool_path(job_application_pool), title: "Edit this pool")
%td= link_to("X", pool_path(job_application_pool), title: "Delete this pool", method: :delete, data: { confirm: "Are you sure?" })
