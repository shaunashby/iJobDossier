class WorkCertificatesController < ApplicationController

  # GET /dossiers/:dossier_id/work_certificates(.:format) dossier_wcert_index
  def index
  end

  # GET /dossiers/:dossier_id/work_certificates/:id(.:format) dossier_wcert
  def show
  end

  # GET /dossiers/:dossier_id/work_certificates/new(.:format) new_dossier_wcert
  def new
  end

  # GET /dossiers/:dossier_id/work_certificates/:id/edit(.:format) edit_dossier_wcert
  def edit
  end

  # POST /dossiers/:dossier_id/work_certificates(.:format)
  def create
  end

  # PUT /dossiers/:dossier_id/work_certificates/:id(.:format)
  def update
  end

  # DELETE /dossiers/:dossier_id/work_certificates/:id(.:format)
  def destroy
  end

end
