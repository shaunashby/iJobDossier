class ReferencesController < ApplicationController

  # GET /dossiers/:dossier_id/references(.:format) dossier_ref_index
  def index
  end

  # GET /dossiers/:dossier_id/references/:id(.:format) dossier_ref
  def show
  end

  # GET /dossiers/:dossier_id/references/new(.:format) new_dossier_ref
  def new
  end

  # GET /dossiers/:dossier_id/references/:id/edit(.:format) edit_dossier_ref
  def edit
  end

  # POST /dossiers/:dossier_id/references(.:format)
  def create
  end

  # PUT /dossiers/:dossier_id/references/:id(.:format)
  def update
  end

  # DELETE /dossiers/:dossier_id/references/:id(.:format)
  def destroy
  end

end
